spring:
  config:
    import:
      - classpath:/frame-it-backend-config/application-local.yaml
      - classpath:/frame-it-backend-config/application-dev.yaml
      - classpath:/frame-it-backend-config/application-prod.yaml
  profiles:
    active: local
  servlet:
    multipart:
      max-file-size: 30MB
      max-request-size: 310MB

server:
  tomcat:
    max-http-form-post-size: 310MB
